fragment NodeFields on Node {
  id
  name
}

query Node($id: ID!) {
  node(id: $id) {
    ...NodeFields
  }
}

mutation insertNode($input: InsertNodeInput!) {
  insertNode(input: $input) {
    ...NodeFields
  }
}
