type Node {
  id: ID!
  name: String!
  includedRootNodes: [Node!]
}

schema {
  query: Query
  mutation: Mutation
}

type Query {
  node(id: ID!): Node
}

type Mutation {
  insertNode(id: ID!, node: NodeInput): Node
}

input NodeInput {
  name: String!
}
